<!-- <div class="container">
  <h1>Welcome to the index of Lists</h1>
  <ul>
    <% @lists.each do |list| %>
      <li>
        <p>
          <%= link_to list.name, list_path(list.id) %>
        </p>
      </li>
    <% end %>
  </ul>
  <%= link_to "Create new list", new_list_path %>
</div> -->
<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY",
      width: 400, height: 300, crop: :fill) %>

<!-- for face detection -->
<%= cl_image_tag("IMAGE_WITH_FACE_ID",
      width: 150, height: 150, crop: :thumb, gravity: :face) %>



 <div class="cover mb-4 d-flex align-items-center justify-content-center">
  <div class="text-center text-white">
    <h1>Save any kind of movies</h1>
    <h3 class="typed-js"></h3>
  </div>
</div>
<div class="container py-5">
  <div class="d-flex align-items-center justify-content-between mb-3">
    <h3 class="mb-0">My Lists</h3>
    <%= link_to 'Create a movie list', new_list_path, class: 'btn btn-flat' %>
   </div>
    <div class="row">
    <% @lists.each do |list| %>
       <div class="co-12 colsm-6 col-md-4">
        <%= link_to list_path(list), class:'text-decoration-none' do %>
           <div class="card-category" data-aos="zoom-in-up" aos-duration="500" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= asset_path('list_background.jpg') %>)">
            <%= list.name %>
          </div>
        <% end %>
       </div>
    <% end %>
  </div>
</div>
